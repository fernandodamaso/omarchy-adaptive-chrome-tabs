# Geometry and arbitration research — FDM-822

This lane determines the exact Omarchy/Quickshell/Hyprland observation contract for adaptive Chrome tab orientation.

The required outcome is one explicit **GO — observation contract** or **NO-GO** decision. Research must pin width units/source, prove long-lived resize detection, qualify eligible versus ambiguous browser surfaces, measure any bounded refresh strategy, and lock multi-window/profile arbitration plus timing semantics.

Remote work may prepare fixtures, fake sinks, pure policy tests, and a deterministic local runbook. Only the target Omarchy machine may claim geometry freshness, compositor cost, or a GO verdict.

Do not invoke a real Chrome-orientation mechanism from this lane.
